export default function UserAddressGeo({geoLat, geoLng}) {

    return (
        <div>
            <div><span>lat:</span> {geoLat} </div>
            <div><span>lng:</span> {geoLng} </div>
        </div>
    );
}